{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "hardhat": "^2.14.0"
  },
  "scripts": {
    "check-balance": "npx hardhat run scripts/check-balance.js --network bscMainnet",
    "deploy-safe": "npx hardhat run scripts/deploy-safe.js --network bscMainnet",
    "verify-deployment": "npx hardhat run scripts/verify-deployment.js --network bscMainnet",
    "deploy-testnet": "npx hardhat run scripts/deploy-safe.js --network bscTestnet"
  }
}
